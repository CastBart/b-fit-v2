bfitv2 Notes
1. carousel of exercise thums is moving when exercises are dragged and dropped - look at b-fit implementation to check for a fix
2. sets cannot be edited if its not the active set - same fix as above
3. exercise details drawer is not present in the session
	- this also includes superset drawer
4. Timer is not present when a set is completed



 next thing regarding the set logger: 1 the header of the set logger, its last colums says "action", lets make this a spanner icon and when pressed it opens a set settings drawer, this drawer will display number of sets and on either side a minus and aplus button which  
  adds a set, also a button to undolast set and a close button. the current exercise menu shows this but i want to add different exercise menu, this button shoul open exercise options drawer, i inlcuded an example in                                                         
  docs/examples/session/options_drawer_and_set_drawer.tsx  




Create new exercises:
1. THE exercise list must pull all system exercises and the ones created by the user.

Drawers:
1. All drawers have 


Active plan:
Need a dashboard section for the active plan.
	- Should show current week. - more notes on weeks below.
	- this section should show the days within the plan
		- Each day card should be split into column of 3
			- Day number
			- Name of the days label. if no label provided it should just say "Day [day number]"
			- Exercises section (show first letters of first 3 exercises, if the day exceeds 3 exercises, then show a +[number of how many more exercises there is])
		- Clicking on the card will open a drawer with the Day details such as
			- Label
			- beside the label, three dots for options
				- once clicked, an options options drawer should appear with following options:
					- Start; this button will start the live workout session using its exercises as session data.
					- Skip; this button marks the day as complete for the week selected.
					- Edit Plan; this button brings the user to the plan builder for this plan. while bringing the user to the plan builder, make sure that the day that the user chose is preselected in th builder
			- Name of the plan it is from 
			- Current number week
			- number of exercises in this day and total number of sets.
			- Start; this button will start the session from this day using its exercises as session data.

	- Week Notes logic:
		- We have the functionality of adding number of weeks into the plan. The idea here is that once a plan is created, the user can track his day workouts for a given week. Each plan stared at first is beginning at week 1. when all day workouts are complete or skipped then the week is marked as complete moving to the next week. There are some rules here as we allow a user to create unlimited duration or 1-52 weeks. when a user selects a number, this number of weeks should be created and each day should be assigned to its own week as this is how we will track the active week, If the user selects unlimited weeks, then the weeks should by created dynamically once all day workouts are completed or skipped. we will use the weeks logic for tracking purpose as the user will be able to open a given week and click on the completed day which will open a drawer displaying the information of that session.
		- The important things here is that the days when started lead us to the session page so for all this logic to work we need a mechanisim for all of this to communicated. can you devise a plan on how we can achieve all this.

Notes:
- The current implementation of creating weeks is the logic I need for when user selected Unlimited option. When a user selects 1-52 weeks, that number of weeks should be created.



PT > Client relations
PT Role findings:
- PT Workouts:
	- PT should be able to assign a workout to a client from the clients page or create one for them there and then
	- currently the PT can only assign existing workouts from the PTs library of workouts
	- When PT assigns a workout from his library, the assigned workout is not displayed on the workoouts tab in the /cleints/[id] > workouts tab. once workouts are assigned to a client, they should show on the workouts tab.
	- PTs should be able to create a workout for the client but it doesnt necessary create a workout for the PT. we need some UI for creating Client Workouts without creating a Wokout for PT User
	- PTs should be able to edit an assigned workout. Since assigning a workout to a client performs a "copy" workout action and Client is considered as the "creator". Once a PT assigns a workout, they should also be able to edit this workout within the clients page
- PT Plans:
	- PT should be able to assign a plan to a client from the clients page or create one for them there and then
	- currently the PT can only assign existing plans from the PTs library of plans
	- When PT assigns a plan from his library, the assigned plan is not displayed in the  /cleints/[id] > plans tab. once plans are assigned to a client, they should show on the plans tab.
	- PT should be able to edit plan details for the client assigned plans without modifying his own. EG. PT has a "Push Pull Plan" in his own plans library. He navigates to clients page, assigns the "Push Pull Plan" to the client. The plan is created for the Client as his own copy. As a scenario, lets say that the PT now wants to modify the Plan slightly for more specific use for the client so wee need ability for the PT to change plan, basically giving him access to plan builder within the clients page. Perhaps this can re-use the plans/[id] page but in a full screen drawer. plan builder should also work in a drawer manner. 
	- PT should be able to activate a plan for a client
- Sessions:
	- PT cannot view client session details - when PT is in the clients/[id] page in sessions tab, sessions performed by the client show up but when clicked, nothing happens. When clicked this should show the complete session drawer (re-use components already in the code)
Client Role findings:
- clients view or workout and plan pages:
	- clients can see workouts and plan page which is great.
	- When a client role is visiting a workouts page and navigates to workout/[id] page, the client can currently see edit and delete button. these action buttons should not be visible for CLIENT roles. the only button that they should see is the start workout action button 
	- When a client role is visiting plans page and navigates to plans/[id] page, the client can currently see all action buttons. The client role should NOT see any action buttons as this should be controlled by the PT.