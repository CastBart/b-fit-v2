# Shared Components

This directory contains reusable components used across multiple features.

## Guidelines

- Components here should be feature-agnostic
- Prefer Shadcn UI components from `ui/` when possible
- Document props with TypeScript interfaces
- Include usage examples in component comments

## Examples

- Loading spinners
- Empty state components
- Common form fields
- Data display components
